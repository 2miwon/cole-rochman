from celery import states


# @app.task(bind=True)
# def crawl_trending(self):
#     self.update_state(state=states.RECEIVED)
#     # results = trending.main(additional_queue=additional_queue)
#     return results

